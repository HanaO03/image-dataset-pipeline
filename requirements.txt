# --- HTTP / scraping ---------------------------------------------------------
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.3.0

# --- imaging -----------------------------------------------------------------
Pillow==11.0.0
ImageHash==4.3.1          # perceptual hashing (near-duplicate detection)

# --- database ----------------------------------------------------------------
psycopg[binary]==3.2.3

# --- transform / export ------------------------------------------------------
pandas==2.2.3
pyarrow==18.1.0           # parquet writer

# --- config / CLI ------------------------------------------------------------
pydantic==2.10.3
pydantic-settings==2.6.1
typer==0.15.1
# Transitive, but pinned deliberately: typer 0.15.x calls
# `Parameter.make_metavar()` with click's pre-8.2 signature, and rich >= 14
# moves the console internals typer's help renderer reaches into. Leaving
# either unpinned let pip resolve click 8.4 / rich 15 and `--help` died with
# a TypeError on any fresh install -- including the Docker image.
click==8.1.8
rich==13.9.4
